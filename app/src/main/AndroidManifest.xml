<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.application.littlehabit">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Login">
            <intent-filter>
                <action android:name="com.application.littlehabit.Login"/>
            </intent-filter>
        </activity>
        <activity android:name=".Register">
            <intent-filter>
                <action android:name="com.application.littlehabit.Register"/>
            </intent-filter>
        </activity>
        <activity android:name=".AddHabit">
            <intent-filter>
                <action android:name="com.application.littlehabit.AddHabit"/>
            </intent-filter>
        </activity>
        <activity android:name=".HabitLog">
            <intent-filter>
                <action android:name="com.application.littlehabit.HabitLog"/>
            </intent-filter>
        </activity>

    </application>

</manifest>